{
  "devDependencies": {
    "chalk": "^4.1.0",
    "fs-extra": "^9.0.1",
    "live-server": "^1.2.1",
    "minimist": "^1.2.5",
    "terser": "^5.5.1",
    "yargs": "^16.2.0"
  },
  "scripts": {
    "serve:dev": "live-server --port=5500 src",
    "serve:prod": "live-server --port=5500 dist",
    "build:dev": "node build.js",
    "build:prod": "node build.js --minify --hash"
  },
  "dependencies": {}
}
